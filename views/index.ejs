<%if(toRender !== "index"){%>
<%-include("includes/header") %>
<%}%>

<%console.log(toRender)%>

<!-- {data, toRender} are passed via controllers to res.renderTemplate("index", {toRender: [String], data: [Objects]}) -->

<%if(data){%>
<%-include(toRender, data)%>
<%}else{%>
<%-include(toRender, toRender)%>
<%}%>


<%if(toRender !== "index"){%>

<!-- flash message wrapper -->

<%-include("includes/flashMessage")%>
<%-include("includes/footer") %>
<%}%>